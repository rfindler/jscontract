Pull requests accepted so far:

- `hex2dec`
- `hex-rgba`
- `natural-compare`

Open pull requests:

- `checksum`
- `file-exists`
- `ms`
- `buffer-equal`

Packages to go through:

- `7zip-min` types
- `to-absolute-glob` types
